import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  avatar: {
    borderRadius: 9999,
    borderWidth: 3,
    borderColor: "#fff",
  },

  avatarWrapper: {
    flexDirection: "row",
    alignItems: "center",
  },
});
